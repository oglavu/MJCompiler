program MJProgram 

class A {
	int broj;
	{
		void meth(int broj) {print(broj);}
	}
}

class B {
	A a;
	{
	int meth(int x, int y, int z) { print(x); return x; } 
	void call_meth(char c) { meth(ord(c), 2, 3); }
	}
}

{

	void main() B b; A a; int c; {
		
		b = new B;
		b.a = new A;
		//c = meth(2,3);
		b.call_meth('A');
		
		return;
	}
}